'use strict';

/**
 * slider-desktop controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::slider-desktop.slider-desktop');
